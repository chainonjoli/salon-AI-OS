# AIコンテンツファクトリー — テスト・デバッグ記録（工程6・7）

> 実施日：2026-07-29　結果：**全74テスト合格**（Worker 25件＋UI E2E 49件）
> ※Claude／Gemini両対応・かんたん接続・コピペ方式の追加後に再実施

## テスト方法

- **Worker（server/worker.js）**：Node 22 で単体テスト。Anthropic APIはスタブ化し、
  リクエスト組み立て・バリデーション・JSON解釈・既存AI受付の非破壊を検証
- **アプリ（factory.html）**：Playwright（Chromium・iPhoneサイズ 390×844・タッチ有効）でE2E。
  デモモードとAIモード（モックWorker）の両方を通しで操作

## テスト項目と結果

### Worker（25件・全合格）

| 項目 | 結果 |
|---|---|
| `ping` が `{ok, provider}` を返す（Claude／Gemini／鍵なしの3パターン） | ✅ |
| 両方の鍵がある場合：既定はClaude、`PROVIDER=gemini` でGeminiに切替 | ✅ |
| **Gemini**：generate が8種を返す／鍵はヘッダー送信／プロフィールが system_instruction に入る／既定モデル gemini-2.5-flash | ✅ |
| **Gemini**：extract がPDFを `inline_data` で送り商品を返す | ✅ |
| **Gemini**：受付チャットが動く（assistant→model変換）／安全ブロック時は定型文 | ✅ |
| テーマ・商品なしの `generate` は 400 | ✅ |
| `generate` が8種のコンテンツJSONを返す（コードフェンス混入も許容） | ✅ |
| システムプロンプトに店舗プロフィール・薬機法ルールが入る | ✅ |
| ユーザーメッセージにテーマ・商品情報が入る | ✅ |
| `extract` の非対応ファイル形式は 400 | ✅ |
| `extract`（PDF）が商品7項目を返す／documentブロックで送信 | ✅ |
| `extract`（画像）は imageブロックで送信 | ✅ |
| 不明なactionは 400 | ✅ |
| **既存のAI受付チャットが壊れていない**（回帰テスト） | ✅ |

### アプリ E2E（49件・全合格）

| 画面 | 項目 |
|---|---|
| ホーム | 大ボタン4つ／お試しモード表示／登録件数バッジ |
| 設定 | 店名・業種・文体の自動保存／ホームへの即時反映／接続テスト成功表示（接続先のAI名＝Claude/Geminiを表示） |
| 商品 | 空状態表示／手入力7項目の保存・一覧表示／商品名必須チェック |
| 作る | 未入力ブロック／ヒントチップでテーマ入力／商品チップ選択 |
| 結果 | 8種カード生成／テーマ・店名・商品の反映／お試しモード表記 |
| コピー | ワンタップコピー（クリップボード実検証）／✓表示／まとめてコピーに8種全部 |
| 履歴 | 自動保存／一覧→再表示→削除ボタン／リロード後もデータ保持 |
| AIモード | 接続後にお試し表示が消える／Worker応答が画面に出る／プロフィールが送信される |
| PDF取込 | ファイル選択→AI抽出→フォーム自動入力 |
| かんたん接続 | 鍵の貼り付け→接続テスト表示／鍵がヘッダー送信される／8種生成／システムプロンプトに店舗情報とNGルール／PDF抽出／お試し表示が消える |
| コピペ方式 | 未入力時のブロック／指示文にテーマ・店舗情報・NGルール・8つの見出しが入る／返事の取り込み（`**`・`##` 等の装飾見出しも解釈）／前置き文の除去／未入力・解読不能時のエラー表示／履歴に「（コピペ）」表示 |
| 全画面 | JSエラーゼロ |

## デバッグ記録（発見→修正済み）

| # | 不具合 | 原因 | 修正 |
|---|---|---|---|
| 1 | AI接続後も「お試しモードです」の帯が消えない | `.home-note{display:flex}` がHTMLの `hidden` 属性（UAスタイル）より優先されていた | 全体ルール `[hidden]{display:none!important}` を追加 |
| 2 | 全画面でfavicon 404のコンソールエラー | favicon未指定 | SVG絵文字faviconをdata URIで埋め込み |
| 3 | デモ生成のLINE配信文で構文エラー | テンプレート内に全角括弧が混入 | 半角に修正（コードレビューで発見） |

## 手動確認

- スクリーンショットでUI目視確認（ホーム／設定／商品登録／作る／結果／履歴／PDF取込）
- 白基調・大ボタン・トースト通知が既存ページ（index.html）とトーン一致

## 未実施（実機での確認を推奨）

- 実際のCloudflare Worker＋Anthropic APIキーでの本番生成（設置手順：docs/06）
- iOS Safari実機でのクリップボード動作（フォールバック実装済み）
- 実物の商品カタログPDFでの抽出精度確認
