# Project Handoff

> このドキュメントは、開発を別のアカウント／別のAIセッションへ引き継ぐためのものです。
> **作業を始める前に必ず全体を読んでください。**特に §12「Do Not Break」と §7「Important Decisions」は、
> 知らずに変更すると設計思想そのものを壊します。
>
> - 最終更新: 2026-08-08
> - 対象リポジトリ: `chainonjoli/salon-AI-OS`（branch: `main`）
> - 【要確認】と書かれた項目は、コードから確定できず推測を含みます。オーナーへの確認が必要です。

---

## 1. Project Overview

### サービス名
**Salon AI OS**

### 目的
個人サロン（エステ・美容室・ネイル・アイラッシュ・整体・リラクゼーション）が
「AIスタッフが24時間働くサロン」を実現するためのプラットフォーム。

**これはホームページ制作ツールではありません。** ここを取り違えると設計が根本から狂います。
ホームページはあくまで結果であり、本体は「サロン固有のナレッジを共有して働く5つのAI部署」です。

### 想定ユーザー
2種類います。両者を混同しないでください。

| | エンドユーザー | 顧客（＝購入者） |
|---|---|---|
| 誰 | サロンに来店するお客様 | 個人サロンのオーナー（40〜60代・ITが苦手） |
| 見る画面 | `index.html` | `admin.html` / `editor.html` / `factory.html` |
| 接点 | LINE・HP経由 | 商談 → `intro.html` |

### 解決したい課題（オーナー側）
1. 営業時間外の問い合わせに翌朝まで気づけない
2. 「どのコースが合うか」を毎回いちから説明している
3. SNSに何を書くか決められず投稿が止まる
4. 顧客情報が紙のカルテと記憶の中にしかない

### 提供価値
サロンの情報を**1か所（ナレッジ）に書くだけ**で、受付・カウンセリング・SNS発信・顧客フォロー・
スタッフ教育の5領域を、サロン自身の言葉でAIが担当する。

### 主要な利用フロー
```
【お客様】  HP/LINE → index.html → AI受付で質問 or AIカウンセラーで3問診断
                                 → LINE友だち追加 / 予約
【オーナー】 editor.html でナレッジ入力 → salon-config.js を書き出し → 差し替え → 全AIに反映
            factory.html でテーマ1つ入力 → SNSコンテンツ8種を下書き → 確認して自分で投稿
【購入検討者】intro.html → 4つのデモを実際に触る → LINEで問い合わせ
```

### 最終的に実現したい状態
**オーナー（chainonjoli）が、このシステムを他のサロンへ販売できる状態。**
本人のサロンは「導入0号店」であり、最終目標は自店の運用ではなく**テンプレート販売／SaaS化**です。
この一点が、すべての設計判断（特に §7-1 ナレッジ・ファースト）の理由になっています。

---

## 2. Current Status

> **2026-08-08 夜 追記：マルチテナント・ライト（M1）本番稼働開始。**
> Cloudflare D1（`tenants`／`usage_daily`）が有効になり、`?s=サロンID` で
> 購入者ごとのサロンが動く。運用手順は `docs/07_tenants.md`。
> 稼働中テナント：`chainonjoli`（導入0号店）／`air-rich`（共同開発モニター第1号・
> 津市のドライヘッドスパ「Air Rich」・実在サロン・接続診断6項目で分離を確認済み）。
> 購入者は設定リンク（`editor.html?s=…&k=…`）から自分のサロンだけを編集でき、
> 保存は1分以内にAI回答へ反映される。以下の本文は同日昼時点の記述。

### 総合完成度：**約 45%**（販売開始に必要な水準に対して）

内訳の考え方 —
- **見せる**（デモとして商談で通用する）：約 85%
- **売れる**（1店舗に納品して運用できる）：約 45%
- **SaaSとして複数店舗に売る**：約 15%

### 一言でいうと
**「AI受付は本番稼働中（2026-08-08〜）。ただしデータはまだ保存されない」状態。**

中核である「本物のAIがサロンのナレッジを使って自由に会話する」部分は、
Gemini（gemini-3.6-flash・有料）＋Cloudflare Workerで接続済み。
`check.html` の受け入れ検査（接続／営業時間／料金／医療的断定／未登録メニュー）に合格しています。
管理画面の保存・カルテ等のデータ永続化は引き続き未実装です。

### 5段階分類

| 分類 | 対象 |
|---|---|
| **【完成】** | お客様ページ全6画面／AI美容カウンセラー（ルールベース診断）／設定エディタ＋ライブプレビュー／業種プリセット5種／販売用紹介ページ `intro.html`／Cloudflare Workerのコード／設計ドキュメント12本／GitHub Pagesデプロイ |
| **【ほぼ完成】** | AI受付（デモモードで動作。実AI接続は未検証）／AIコンテンツファクトリー（同上）／レスポンシブ対応／レート制限・オリジン検証 |
| **【作業中】** | 管理画面 `admin.html`（**UIモックアップ。保存は動作しない**）／販売資料まわり（料金未定） |
| **【未実装】** | データ永続化／ログイン認証／AI秘書のロジック／AI教育担当のロジック／予約システム／決済／マルチテナント／アクセス解析／SEO |
| **【要確認】** | 実AI接続時の応答品質・コスト／Anthropic・Google側の月額上限設定／`hp` リポジトリの既存GASチャットボットとの重複 |

---

## 3. Architecture

### 技術構成
**依存ライブラリゼロ・ビルド工程ゼロ**の素のHTML/CSS/JavaScript。

- フレームワーク：**なし**（意図的。§7-2 参照）
- パッケージマネージャ：**なし**（`package.json` は存在しません）
- ビルド：**なし**（`index.html` をブラウザで開けばそのまま動く）
- CSS：各HTMLファイル内の `<style>`。CSS変数でテーマ化
- 配信：GitHub Pages（`main` ブランチのルート）
- サーバー：Cloudflare Workers 1本のみ（AIのAPIキーを隠すためだけに存在）
- データベース：**なし**（設計のみ `docs/03_database_design.md`）

### ディレクトリ構成
```
salon-AI-OS/
├── index.html                  お客様ページ（エンジン本体・6画面）
├── admin.html                  オーナー管理画面（PINゲート・現状モックアップ）
├── editor.html                 設定エディタ（フォーム→ライブプレビュー→書き出し）
├── factory.html                AIコンテンツファクトリー（AI広報の実装）
├── intro.html                  販売用の商品紹介ページ
├── check.html                  AI受付の接続診断（受け入れ検査5項目・noindex）
├── wrangler.jsonc              Cloudflare Git連携の設定（Worker本体と運用変数の指定）
├── salon-config.js             ★ chainonjoli のナレッジ（実データ）
├── salon-config.template.js    新規サロン用の空テンプレート
├── presets.js                  業種プリセット5種（美容室/ネイル/アイラッシュ/整体/リラクゼーション）
├── server/worker.js            Cloudflare Worker（Claude / Gemini 両対応）
├── docs/01〜06                 設計書（画面遷移・構成・DB・ナレッジ・ロードマップ・設置手順）
├── docs/10〜14                 コンテンツファクトリーの要件・機能・UI・データ・テスト
├── README.md
└── HANDOFF.md                  ← このファイル
```

### 最重要ファイル（この2つの関係が本システムの核心）

| ファイル | 役割 |
|---|---|
| `index.html` | **エンジン**。サロン固有の情報を1文字も持たない。すべて `C.xxx` として設定から読む |
| `salon-config.js` | **ナレッジ**。`window.SALON_CONFIG` に全サロン情報。これを差し替えれば別サロンになる |

`salon-config.js` のトップレベルキー：
```
brand / theme / ai / admin / contact / home / menu / access / reserve /
reception / counselor / marketing / quiz
```

### 画面ID一覧（変更するとナビゲーションが壊れます）
- `index.html` … `c-home` `c-menu` `c-counsel` `c-chat` `c-access` `c-reserve`
- `admin.html` … `a-home` `a-kn` `a-kn-faq` `a-mkt` `a-crm` `a-crm-detail` `a-edu` `a-analytics`
- `factory.html` … `f-home` `f-create` `f-result` `f-products` `f-product-edit` `f-history` `f-settings`

### データの流れ
```
editor.html ──postMessage({type:'salon-config'})──▶ index.html(iframe) ライブプレビュー
     │
     └─ 書き出し ─▶ salon-config.js ─▶ index.html / admin.html が読み込む

index.html ──POST {dept:'reception', messages:[...]}──▶ Worker ──▶ Claude or Gemini
factory.html ──POST {dept:'factory', action:'generate'|'extract'|'ping'}──▶ Worker ──▶ 同上
```

---

## 4. Implemented Features

### お客様ページ `index.html` 【完成】
- 6画面＋下部タブナビゲーション。全画面3タップ以内で到達
- **URLハッシュによるディープリンク**：`#counsel` `#chat` `#menu` `#access` `#reserve` `#home`
  - 実際に `hp` リポジトリから `.../salon-AI-OS/#counsel` で直リンクされています（壊すと外部リンク切れ）
  - 履歴を汚さないよう `history.replaceState` を使用
- LINE友だち追加・電話発信の導線
- `postMessage` によるエディタからのライブ更新受信
- ページタイトルは `hydrate()` が実行時に `サロン名｜タグライン` へ書き換え

### AI美容カウンセラー 【完成】
- 3つの質問（悩み → 期間 → 現在のケア）→ 結果3ブロック（施術／ホームケア／生活習慣）
- **重要：これはAIを呼んでいません。100%クライアント側のルールベース**です。
  `salon-config.js` の `counselor.concerns` を引いているだけ。
  そのぶんコストゼロ・応答即時・出力が絶対に暴れないという利点があります（§7-4）
- 結果画面に医療免責文（`counselor.disclaimer`）を常設

### AI受付（チャット） 【ほぼ完成】
- `salon-config.js` の `ai.endpoint` が **空文字なら「デモモード」**
  - FAQチップのタップのみ。自由入力欄（`#chat-form`）は `hidden`
- `ai.endpoint` に Worker のURLを入れると **「AIモード」**
  - 自由入力欄が出現し、直近12往復を Worker へPOST
  - 通信失敗時はLINE・電話への案内にフォールバック
- **2026-08-08より本番接続済み**：Cloudflare Worker（Git連携デプロイ・`wrangler.jsonc` 参照）＋
  Gemini（`gemini-3.6-flash`・有料）。接続診断ページ `check.html` の5項目検査で合格を確認済み。
  受付は `thinkingLevel:'low'` で思考トークンを抑えてコスト管理している

### 設定エディタ `editor.html` 【完成】
- フォーム編集 → 右のiframeへ即時反映（`postMessage`）
- 編集中の内容を `localStorage` に自動下書き保存
- **業種プリセット5種を1クリックで読み込み**（上書き前に `confirm` で確認）
- `salon-config.js` としてダウンロード書き出し
- 編集できる範囲：`brand` / `theme`(パレット選択) / `contact` / `home.cards` / `menu` /
  `access` / `reserve` / `reception.greeting` / `reception.faq` / `counselor.concerns` /
  `counselor.disclaimer` / `marketing` / `quiz`
- **編集できない範囲：`ai.endpoint` と `admin.pin`**（手でファイルを編集する必要あり。§11 既知の課題）

### AIコンテンツファクトリー `factory.html` 【ほぼ完成】
- テーマ1つ → 8種のコンテンツを一括生成
  （Instagram投稿／リール台本／LINE配信文／ブログ／メルマガ／ショート動画台本／キャプション／ハッシュタグ）
- PDF・写真から商品情報をAIが読み取って登録（`action:'extract'`）
- 商品カタログ・生成履歴を `localStorage` に保存
- 薬機法NGワードをシステムプロンプトで抑制
- **接続先は `salon-config.js` ではなく、factory.html 内の設定画面で別途入力する `localStorage` の
  `factory.profile.aiEndpoint`**（§11 既知の課題）

### 業種プリセット `presets.js` 【完成】
`美容室` `ネイル` `アイラッシュ` `整体` `リラクゼーション` の5種。
（エステは `salon-config.js` 本体が実例を兼ねています）
各プリセットは業種固有の悩み項目・FAQ・投稿サンプル・研修クイズ・**業種別の免責文**を持ちます。
特に `整体` の免責文は「医療機関ではない・治療ではない・強い痛みやしびれは整形外科へ」と最も強い表現です。

### AIワーカー `server/worker.js` 【完成（コードとして）】
- **Claude と Gemini の両対応**。`PROVIDER` で切替、未設定なら鍵があるほうを自動選択
- プロンプトキャッシュ（systemブロックに `cache_control: ephemeral`）
- Claude利用時は `output_config: {effort:'low'}`（受付は短文・低遅延でよい）
- `stop_reason === 'refusal'` を検知して定型文へ
- **レート制限**：同一IP 1分あたり `RATE_PER_MIN`（既定8）／全体 1日あたり `RATE_PER_DAY`（既定500）
- **オリジン検証**：`ALLOWED_ORIGIN` 設定時は不一致を403
- 入力検証：直近12往復・各2000字まで
- ※レート制限はWorkerインスタンスのメモリ上のため厳密ではありません。
  **必ずAnthropic／Google側でも月額上限を設定してください**（§11）

### 販売用紹介ページ `intro.html` 【完成】
悩み → 5つのAI部署 → ナレッジ → 触れるデモ4種 → 導入3ステップ → 対応業種 →
安全方針 → 問い合わせ、の構成。料金は「お問い合わせ」表記（金額未定のため）。
「体験版のためAIは決まった受け答え」「カルテ・分析はサンプル」と明記済み。

### 横断項目

| 項目 | 状態 |
|---|---|
| レスポンシブ | 【完成】スマホファースト。PCではスマホフレーム内に表示。横スクロールなし確認済み |
| エラー処理 | 【ほぼ完成】AI通信失敗→LINE/電話へフォールバック。Worker側は502/403/400を返す |
| セキュリティ | 【作業中】APIキーはWorker側で秘匿済み。ただし管理画面はPINのみ（本物の認証ではない） |
| デプロイ | 【完成】GitHub Pages（`main` push で自動） |
| データ保存 | 【未実装】サーバー保存は一切なし。`localStorage`/`sessionStorage` のみ |
| SEO | 【未実装】`index.html` に description / OGP なし（titleのみ実行時設定） |
| アクセス解析 | 【未実装】計測タグ一切なし |

---

## 5. In Progress

### 管理画面 `admin.html`
**現状は動くUIモックアップです。ここを誤解しないでください。**
- 「保存して、5つのAIに反映する」ボタンは、実際には `toast()` を出すだけで**何も保存しません**
- 画面上には「保存すると即時に反映され、次の会話からAI受付がこの内容で回答します」と
  書かれていますが、**これは実装されていません**（商談デモ用の文言）
- 顧客カルテ（佐藤美咲 等）・分析の数値は**すべて架空のサンプル**
- PINゲートは `sessionStorage` のフラグのみ。**覗き見防止であって認証ではありません**

実際にナレッジを編集する手段は、現状 `editor.html` だけです。

### 販売準備
`intro.html` は公開済みですが、**料金が未定**のため「お問い合わせください」のままです。

---

## 6. Remaining Tasks

### Priority A：販売開始に必須

| # | タスク | 補足 |
|---|---|---|
| A-1 | ~~AI受付を実接続して動作検証する~~ **完了（2026-08-08）** | Gemini（gemini-3.6-flash・有料）＋Cloudflare WorkerのGit連携で接続。`check.html` の診断5項目で合格。回数上限は `wrangler.jsonc` の `RATE_PER_DAY=60` |
| A-2 | **API側の月額上限を設定する** | Anthropic Console → Settings → Limits。Gemini の場合は Google Cloud の予算アラート。**オーナー本人のログインが必要なためAIでは実行できません** |
| A-3 | **`admin.pin` を変更し、公開ページから削除する** | 現在 `intro.html` にデモ用PINとして実PINが記載。実顧客データを入れる前に必須 |
| A-4 | **`worker.js` のナレッジ二重管理を解消する** | `worker.js` 内の `SALON` 定数が `salon-config.js` の内容を手動コピーしている。価格を変えると片方だけ古くなる（§11-1） |
| A-5 | **料金を決めて `intro.html` に載せる** | 「まずご相談」より金額が見えるほうが問い合わせは増える |
| A-6 | **管理画面の「保存」を実装するか、デモである旨を明示する** | 現状は「できると書いてあるができない」状態。納品時に必ず問題になる |

### Priority B：完成度向上

| # | タスク |
|---|---|
| B-1 | `index.html` に description / OGP / 構造化データを追加（SEO） |
| B-2 | `editor.html` に `ai.endpoint` と `admin.pin` の入力欄を追加（今は手編集が必要） |
| B-3 | `editor.html` / `factory.html` に `noindex` を付与（オーナー用ツールが検索に出る） |
| B-4 | `factory.html` の接続設定を `salon-config.js` の `ai.endpoint` と統一 |
| B-5 | アクセス解析の導入（どの画面が見られているか分からない） |
| B-6 | AI秘書（来店周期の見守り・フォロー下書き）のロジック実装 |
| B-7 | AI教育担当（研修・理解度チェック）のロジック実装 |
| B-8 | デモ専用の管理画面を分離し、本番PINを公開しない構成にする |

### Priority C：将来的な改善（Phase 2〜3）

| # | タスク |
|---|---|
| C-1 | データ永続化（ナレッジ・カルテをサーバー保存）。設計は `docs/03_database_design.md` |
| C-2 | 本物のログイン認証 |
| C-3 | マルチテナント化（`tenant_id`）＝SaaS本格運用 |
| C-4 | 予約システム・決済・EC |
| C-5 | Instagram / LINE API 連携（現状は下書きをコピーして手で投稿） |
| C-6 | 業種プリセットの追加（まつげパーマ・脱毛・ヨガ等） |

---

## 7. Important Decisions

### 7-1. ナレッジ・ファースト（**この設計は絶対に壊さないでください**）
**エンジンのコードにサロン固有の情報を1文字も書かない。**
サロン名・価格・文体・FAQ・悩み項目・免責文、すべて `salon-config.js` に外部化する。

なぜか：**これがテンプレート販売というビジネスモデルそのものだから**です。
「設定ファイルを1枚書けば別のサロンになる」から商品として売れます。
`index.html` に「chainonjoli」や「¥8,800」を直接書いた瞬間、この商品は売り物でなくなります。

新しく機能を足すときも、**表示される文字列は必ず設定経由**にしてください。

### 7-2. フレームワークを使わない
React・Vue・ビルドツールを**意図的に採用していません**。理由：
- 買い手（40〜60代のサロンオーナー）が自分でファイルを差し替えられる
- 納品が「HTMLファイル数枚を置くだけ」で済む
- ビルド環境の劣化・依存パッケージの脆弱性に将来悩まされない

「モダン化」の名目でReact化するのは、この商品にとっては**退化**です。実施しないでください。

### 7-3. お客様画面とオーナー画面を完全に分離した
以前は1つのデモハブから両方に行けましたが、**顧客カルテに誰でも到達できてしまう**ため
オーナーの指示（「分けて」）で分離しました。
`index.html` には管理画面へのリンクを**一切置かない**というのがルールです。復活させないでください。

### 7-4. AIカウンセラーをあえてルールベースにした
3問診断はAIを呼んでいません。これは手抜きではなく判断です。
- コストゼロ・応答が即時
- 出力が絶対に暴れない＝**薬機法・医療広告の観点で安全**
- ナレッジを直せば結果も変わるので、ナレッジ・ファーストの原則とも整合

「AIらしくするために」ここをLLM呼び出しに変えると、コスト・遅延・法的リスクが同時に増えます。
変更する場合は必ずオーナーに確認してください。

### 7-5. Gemini対応を残した理由
Claude だけでなく Gemini も使えるようにしてあります（`PROVIDER`）。
Gemini には無料枠があるため、**買い手のサロンが費用ゼロで始められる**選択肢を残すためです。
Claude 専用に単純化しないでください。

### 7-6. AIの出力は必ず「下書き」で止める
SNS投稿もLINE配信も、AIが自動送信することは**設計上ありません**。
必ずオーナーが確認して自分で投稿します。`intro.html` でも「勝手に送信しません」と約束しています。
自動投稿機能を足す場合は、この約束を破ることになる点をオーナーに明示してください。

### 7-7. オーナーから明示的に指示されたこと
- サロン名は **chainonjoli**（シェノンジョリ）
- 公式LINEは `salon-config.js` の `contact.lineUrl` が現行。**旧アカウントのURLに戻さないこと**
  （過去に全ファイル7か所を新URLへ置換済み）
- 料金は3段階：ビジター ¥8,800 ／ 美白・保湿コース ¥11,000（90分＋パック付き）／ 会員 ¥3,300
  - **過去に ¥8,800 を ¥11,000 で置き換えるミスをして訂正されました。3つとも並存が正です**
- 管理画面のPINは変更済み（実値は `salon-config.js` の `admin.pin` を参照。ここには記載しません）
- 最終目標は **他サロンへの販売**。自店運用が目的ではない

### 7-8. 採用しなかった案
- 顧客画面とオーナー画面の共通ハブ（→ 情報漏洩リスクで廃止）
- `index.html` へのサロン情報の直書き（→ ナレッジ・ファースト違反）
- 管理画面への本格認証の先行実装（→ MVPを優先し Phase 2 へ）

---

## 8. UI / UX Rules

対象ユーザーが「40〜60代・AIが苦手」であることが、すべてのUIルールの根拠です。

### デザイン
- 白基調（`--paper: #FCFBF9`）＋**くすみカラー**。彩度の高い色を使わない
- 余白を広く取る。詰め込まない
- 見出しは明朝体（`Hiragino Mincho ProN` 系）、本文はゴシック体
- アクセント色はサロンごとに変わる。**CSS変数経由で `theme` から注入**（ハードコード禁止）
  - chainonjoli: `#A18072` / 美容室: `#7F94A1` / ネイル: `#B58A8F` /
    アイラッシュ: `#9D8FA8` / 整体: `#7F94A1` / リラクゼーション: `#8FA08A`
  - ※プリセットの色は `editor.html` のパレットに存在する値と一致させること（一致しないと選択が保持されない）

### 導線
- **主要操作は3タップ以内**
- 下部タブは常時表示。今どこにいるか常に分かる
- 戻り先が曖昧な画面を作らない

### 文章
- **専門用語を出さない**。「プロンプト」ではなく「サロンの言葉で書いてください」
- 「AI」という言葉は使ってよいが、技術説明はしない
- 断定・煽り・数値の誇張をしない
- NGワード：`治る` `消える` `医学的に` `絶対` `激安`（`salon-config.js` の `style.ng` で管理）
- 絵文字は1メッセージに1個まで

### ターゲットへの配慮
- AIの提案は必ず**下書き＋承認**。勝手に送信しない
- 「できないこと」は正直に書く。誇張した約束をしない
- 医療行為をしない。症状が続く場合は医療機関の受診をすすめる

### レスポンシブ
- スマホファースト（基準幅 390px）。PCではスマホフレーム内に表示
- 横スクロールを絶対に発生させない
- `env(safe-area-inset-bottom)` を考慮（iPhoneのホームバー）
- `prefers-reduced-motion` に対応

---

## 9. External Services

| サービス | 用途 | 状態 |
|---|---|---|
| **GitHub** | ソース管理 | `chainonjoli/salon-AI-OS`（public / branch `main`） |
| **GitHub Pages** | 本番配信 | 稼働中。`main` への push で自動デプロイ |
| **Cloudflare Workers** | AIプロキシ（APIキー秘匿） | **稼働中**（2026-08-08〜）。GitHubと連携し `main` へのpushで自動デプロイ（設定は `wrangler.jsonc`）。APIキーはダッシュボードのSecret `GEMINI_API_KEY` のみ |
| **Google AI Studio (Gemini)** | AI受付・ファクトリーの頭脳（`gemini-3.6-flash`） | **契約済み・有料・稼働中**。受付は thinkingLevel low でコスト抑制 |
| **Anthropic API** | Claude（コードは対応済み・切替可能） | 未契約。`PROVIDER=claude`＋キー登録で切替できる |
| **LINE公式アカウント** | 予約・問い合わせ導線 | 稼働中（URLは `salon-config.js` の `contact.lineUrl`） |
| **データベース** | — | **未使用**。設計のみ `docs/03_database_design.md` |

### 公開URL
```
お客様ページ          https://chainonjoli.github.io/salon-AI-OS/
オーナー管理画面      https://chainonjoli.github.io/salon-AI-OS/admin.html
設定エディタ          https://chainonjoli.github.io/salon-AI-OS/editor.html
コンテンツファクトリー https://chainonjoli.github.io/salon-AI-OS/factory.html
販売用紹介ページ      https://chainonjoli.github.io/salon-AI-OS/intro.html
```
※ URLのパスは**大文字小文字を区別します**（`salon-AI-OS` であって `salon-ai-os` ではない）。
過去にこれが原因で「404が出る」という時間のかかる切り分けが発生しました。

### 関連する別リポジトリ（このリポジトリの外）

| リポジトリ | 内容 | 注意 |
|---|---|---|
| `chainonjoli/hp` | サロンの実ホームページ | `#counsel` へ直リンクしている。ハッシュ名を変えると切れる。**別途 Google Apps Script のチャットボットが稼働中**（詳細 §11-4） |
| `chainonjoli/poupelle.mie` | 別プロジェクト。`salon-ai-os/` に**古いスナップショット**あり | **こちらを編集しないこと**（§12-6） |

---

## 10. Environment Variables

すべて **Cloudflare Worker のシークレット／変数**として設定します。
リポジトリ内に `.env` は存在せず、コードにキーは一切含まれていません。

| 変数名 | 必須 | 用途 |
|---|---|---|
| `ANTHROPIC_API_KEY` | どちらか | Claude を使う場合のAPIキー（Secret として登録） |
| `GEMINI_API_KEY` | どちらか | Gemini を使う場合のAPIキー（Secret として登録） |
| `PROVIDER` | 任意 | `claude` / `gemini`。未設定ならキーがあるほうを自動選択 |
| `MODEL` | 任意 | Claudeのモデル名。既定 `claude-opus-5` |
| `GEMINI_MODEL` | 任意 | Geminiのモデル名。既定 `gemini-2.5-flash` |
| `ALLOWED_ORIGIN` | 推奨 | 許可するサイトのオリジン。未設定だと全オリジン許可（開発用） |
| `RATE_PER_MIN` | 任意 | 同一IPの1分あたり上限。既定 `8` |
| `RATE_PER_DAY` | 任意 | 全体の1日あたり上限。既定 `500` |

### クライアント側の設定値（環境変数ではありません）
| 場所 | キー | 用途 |
|---|---|---|
| `salon-config.js` | `ai.endpoint` | Worker のURL。**空文字ならデモモード** |
| `salon-config.js` | `admin.pin` | 管理画面の簡易PIN（公開リポジトリから読めます） |
| `localStorage` | `factory.profile.aiEndpoint` | factory.html 専用の接続先（別管理・§11-3） |

> **APIキー・PINの実値をこのファイルやコミットメッセージ、issue、PRに書かないでください。**

---

## 11. Known Issues

### 11-1. ナレッジが二重管理になっている 🔴 重要
`server/worker.js` の先頭にある `SALON` 定数は、`salon-config.js` の内容を**手でコピーしたもの**です。
価格やFAQを変更した際、**片方だけ直すとAI受付が古い情報を答えます。**
現状は「両方を必ず同時に直す」しかありません。Priority A-4 で解消すべき箇所です。

### 11-2. 管理画面の「保存」が動かない 🔴 重要
`admin.html` の保存ボタンはトーストを出すだけです。画面上の説明文は「反映される」と書いていますが
実装されていません。**実顧客に納品する前に、実装するか文言を修正する必要があります。**

### 11-3. factory.html の接続先が別管理
`factory.html` は `salon-config.js` の `ai.endpoint` を読まず、自分の設定画面で入力した
`localStorage` の値を使います。**同じWorkerを2か所に設定する必要があり、片方だけ設定すると混乱します。**

### 11-4. AIチャットが2系統ある【要確認】
`chainonjoli/hp`（実ホームページ）には、**Google Apps Script 経由の別のチャットボットが既に稼働中**です。
Salon AI OS の AI受付とは完全に別系統で、ナレッジも共有していません。
さらに、そのGASのWebアプリURLは公開リポジトリ内に記載されており、
**第三者が直接呼び出して費用を消費できる状態**です（費用が発生する構成かは未確認）。
どちらに一本化するか、オーナーの判断が必要です。

### 11-5. 管理画面のPINが公開情報
`salon-config.js`（publicリポジトリ）と `intro.html`（公開ページ）の両方にPINが記載されています。
**架空のサンプルデータしか入っていない現状では問題ありませんが、実顧客情報を入れる前に必ず変更してください。**
そもそもPINは覗き見防止であり、認証ではありません。

### 11-6. データが一切保存されない
ページを再読み込みすると、`editor.html` の下書きと `factory.html` の履歴以外はすべて消えます。
これは未実装であり、不具合ではありません。

### 11-7. SEO・アクセス解析が未実装
`index.html` に description / OGP がありません。計測タグもありません。
「誰がどれだけ見たか」が分からない状態です。

### 11-8. 過去に発生し、再発させてはいけない不具合
| 症状 | 原因 | 対策 |
|---|---|---|
| ページ起動時に `ReferenceError` | `applyHash()` を `let` 変数の宣言より前に呼んでいた（TDZ） | **`applyHash()` の呼び出しは必ずスクリプト最後尾**。`index.html` 末尾のコメント参照 |
| デモモードなのに入力欄が出る | `#chat-form` にインラインの `style="display:flex"` があり `hidden` を上書き | インラインstyleを使わず `#chat-form[hidden]{display:none}` で制御 |
| スマホで終了ボタンが本文に重なる | 固定配置の衝突 | `@media(max-width:767px)` で下部へ退避＋`safe-area-inset` 考慮 |
| プリセットの色が保存されない | `presets.js` の色が `editor.html` のパレットに無い値だった | プリセットの色は必ずパレット内の値と一致させる |
| GitHub Pages が404 | URLの大文字小文字（`salon-AI-OS`） | パスを手打ちしない |

---

## 12. Do Not Break

**以下は、変更すると設計思想・ビジネスモデル・法的安全性のいずれかを壊します。**

1. **`index.html` にサロン固有の文字列を書かない**
   サロン名・価格・FAQ・文体はすべて `salon-config.js` 経由。これが商品の生命線です（§7-1）。

2. **`index.html` から管理画面へリンクしない**
   オーナーの明示的な指示です。顧客カルテへの導線を作らないこと（§7-3）。

3. **URLハッシュ（`#counsel` `#chat` `#menu` `#access` `#reserve` `#home`）を変えない**
   実ホームページ `chainonjoli/hp` から外部リンクされています。変えるとリンク切れになります。

4. **医療免責の仕組みを外さない・弱めない**
   `counselor.disclaimer` の結果画面常設、NGワード、Workerのシステムプロンプト内の
   「医療行為をしない」原則。特に `整体` プリセットの免責文は最も強い表現が必要です。

5. **AIが自動送信する機能を勝手に足さない**
   「必ず下書きで止まる」は `intro.html` で顧客に約束している内容です（§7-6）。

6. **`chainonjoli/poupelle.mie` の `salon-ai-os/` フォルダを編集しない**
   あれは古いスナップショット（`admin.html` `factory.html` `presets.js` `intro.html` を含まない）です。
   **正となるのは `chainonjoli/salon-AI-OS` リポジトリだけ**です。
   `poupelle.mie` の PR #57 はこの旧版のもので、現在は役目を終えています。

7. **React等への移行・ビルド工程の追加をしない**（§7-2）

8. **APIキー・PINをコードやドキュメント、コミットメッセージに書かない**

9. **既存のCSS変数名・画面ID・`window.SALON_CONFIG` の構造を安易に改名しない**
   `editor.html` `presets.js` `admin.html` が同じ構造に依存しています。1か所変えると連鎖して壊れます。

---

## 13. Recommended Next Actions

次のセッションで、**この順番**で進めることを推奨します。

### ステップ0：把握（コードは触らない）
1. この `HANDOFF.md` を最後まで読む
2. `README.md` と `docs/04_ai_knowledge.md`（ナレッジ構造）を読む
3. `https://chainonjoli.github.io/salon-AI-OS/intro.html` を実際にスマホで開き、
   4つのデモをすべて触って現状を体感する
4. `salon-config.js` と `index.html` を並べて読み、「エンジンと設定の分離」を確認する

### ステップ1：オーナーに確認する（作業を始める前に）
- Anthropic か Google AI Studio、**どちらのアカウントを作るか**（Geminiは無料枠あり）
- **料金をいくらにするか**（`intro.html` に載せるため）
- `hp` の既存GASチャットボットを**残すか、Salon AI OS に一本化するか**（§11-4）
- 管理画面の「保存」を**実装するか、デモと明示するか**（§11-2）

### ステップ2：Priority A を上から順に
1. **A-1 AI受付の実接続**（`docs/06_deploy.md` の手順どおり。約30分・コピペのみ）
   - 接続後、必ず実機で会話を検証する。価格・営業時間を正しく答えるか、
     NGワードを使わないか、知らないことを創作しないかを確認
2. **A-2 月額上限の設定**（オーナー本人の作業。AIでは代行できません）
3. **A-4 `worker.js` のナレッジ二重管理を解消**（A-1の直後が最適。この時点なら影響範囲が小さい）
4. **A-3 PIN変更＋`intro.html` からの削除**
5. **A-6 管理画面の保存の扱いを決着**
6. **A-5 料金の掲載**

### 変更するときのお願い
- **1コミット1目的**にしてください（このリポジトリはレビューが人力です）
- コミットメッセージは日本語。オーナーが読みます
- 変更後は必ず `intro.html` からの4つのデモを一通り触って、壊れていないことを確認してください
- テストコードはありません。**目視確認が唯一の検証手段**です

---

## 14. Verification Checklist

引き継ぎ直後に、以下を上から順に確認してください。

### リポジトリ
- [ ] `chainonjoli/salon-AI-OS` をクローンできた（`salon-ai-os` ではなく `salon-AI-OS`）
- [ ] `git branch` が `main` である
- [ ] `git status` がクリーンである
- [ ] `git log --oneline -5` の最新が `販売用の紹介ページ（intro.html）を追加` 以降である
- [ ] ファイルが23個そろっている（`git ls-files | wc -l`）

### 動作確認（ローカル）
- [ ] `index.html` をブラウザで開くと、chainonjoli のページとして表示される
- [ ] 下部タブで6画面すべてに移動できる
- [ ] `#counsel` を付けて開くと、いきなりカウンセラー画面が出る
- [ ] AIカウンセラーの3問に答えると、結果と免責文が出る
- [ ] チャット画面で**自由入力欄が出ていない**（＝デモモードで正しい）
- [ ] `editor.html` でサロン名を変えると、右のプレビューが即座に変わる
- [ ] `editor.html` で業種プリセット（例：整体）を読み込むと、色・悩み項目・免責文が変わる
- [ ] `admin.html` がPINを要求する

### 公開環境
- [ ] 5つの公開URL（§9）がすべて開ける
- [ ] `intro.html` の4つのデモリンクがすべて開ける
- [ ] `hp` の「AIに相談してみる」ボタンからカウンセラーに飛べる

### 安全確認
- [ ] リポジトリ内にAPIキーが無い（`grep -rEi "sk-ant|AIza[A-Za-z0-9_-]{20,}" .` が空）
- [ ] `salon-config.js` の `ai.endpoint` にWorkerのURLが入っている（＝本番稼働中。**課金が発生する状態**）
- [ ] `check.html`（接続診断）を実行し、5項目が ✅/⚠️ である
- [ ] Google Cloud側の予算アラートが設定済みか、オーナーに確認した（通知のみで自動停止しない点も理解しているか）

### 理解の確認（新しいAIへの問い）
- [ ] 「このシステムの最終目標は？」→ **他サロンへの販売**と答えられる
- [ ] 「新しいサロン用にカスタマイズするには？」→ **`salon-config.js` を書き換える。コードは触らない**
- [ ] 「AIカウンセラーはAIを呼んでいる？」→ **呼んでいない。ルールベース**
- [ ] 「管理画面の保存ボタンは動く？」→ **動かない。モックアップ**
